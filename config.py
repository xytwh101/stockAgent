"""
config.py — 全局配置，所有常量、权重、阈值的唯一来源
无业务逻辑，只有数据
"""
import os
from dotenv import load_dotenv

load_dotenv()

# ─────────────────────────────────────────────
# API 配置
# ─────────────────────────────────────────────
API_CONFIG = {
    "fmp_api_key": os.getenv("FMP_API_KEY", ""),
    "fmp_base_url": "https://financialmodelingprep.com/api/v3",
    "fmp_rate_limit_per_minute": 250,   # FMP 基础版限速
    "request_timeout_seconds": 30,
    "max_retries": 3,
    "retry_backoff_seconds": 2,
}

# ─────────────────────────────────────────────
# 本地缓存配置
# ─────────────────────────────────────────────
CACHE_CONFIG = {
    "db_path": "data/stock_cache.db",
    "financial_data_ttl_days": 90,   # 财务报表：季报才更新
    "valuation_data_ttl_days": 7,    # 估值数据：价格每天变
    "universe_ttl_days": 30,         # 股票列表：月度刷新
}

# ─────────────────────────────────────────────
# 三道过滤漏斗参数
# ─────────────────────────────────────────────
FUNNEL_CONFIG = {
    # 第一道：基础过滤（市值/流动性/交易所）
    "stage1_min_market_cap_usd": 300_000_000,    # 最低市值 $3亿
    "stage1_min_avg_daily_volume_usd": 500_000,  # 最低日均成交额 $50万
    "stage1_min_listing_years": 3,               # 上市年限至少3年
    "stage1_exchanges": ["NYSE", "NASDAQ", "AMEX"],

    # 第二道：财务质量筛选
    "stage2_min_profitable_years": 2,    # 过去3年至少2年盈利
    "stage2_max_debt_to_assets": 0.85,   # 总负债/总资产 < 85%
    "stage2_lookback_years": 3,

    # 第三道：全量打分（无额外过滤，仅一票否决）
}

# ─────────────────────────────────────────────
# 一票否决项（任一触发 → 标红，不参与排名）
# ─────────────────────────────────────────────
VETO_CONDITIONS = {
    "fcf_negative_years": 3,           # 连续N年自由现金流为负
    "net_loss_consecutive_years": 2,   # 净利润率<0 且连续N年
    "debt_to_fcf_max": 10.0,           # 长期负债/FCF > 10（偿债需10年以上）
    "insider_selling_threshold": 0.30, # 近1年内部人减持 > 30%
    # 审计非标准意见：数据标记字段 audit_opinion != "Unqualified"
}

# ─────────────────────────────────────────────
# 五大评分维度默认权重
# ─────────────────────────────────────────────
DIMENSION_WEIGHTS = {
    "business_quality": 0.30,
    "financial_health": 0.20,
    "growth":           0.20,
    "management":       0.15,
    "valuation":        0.15,
}

# ─────────────────────────────────────────────
# 四位大师权重矩阵
# ─────────────────────────────────────────────
MASTER_WEIGHTS = {
    "buffett": {
        "business_quality": 0.40,
        "financial_health": 0.20,
        "growth":           0.10,
        "management":       0.20,
        "valuation":        0.10,
        # 核心哲学：护城河 + 现金流
    },
    "munger": {
        "business_quality": 0.35,
        "financial_health": 0.20,
        "growth":           0.10,
        "management":       0.30,
        "valuation":        0.05,
        # 核心哲学：激励机制 + 逆向思考
    },
    "duan": {
        "business_quality": 0.35,
        "financial_health": 0.15,
        "growth":           0.25,
        "management":       0.25,
        "valuation":        0.00,
        # 核心哲学：本分文化 + 10年后（不依赖估值选股）
    },
    "lynch": {
        "business_quality": 0.20,
        "financial_health": 0.15,
        "growth":           0.40,
        "management":       0.10,
        "valuation":        0.15,
        # 核心哲学：PEG成长 + 被忽视
    },
}

# ─────────────────────────────────────────────
# 打分阈值：生意质量
# ─────────────────────────────────────────────
BUSINESS_QUALITY_THRESHOLDS = {
    # ROE（过去5年均值）
    "roe": [
        (0.25, 10), (0.20, 8), (0.15, 6), (0.10, 4), (0.0, 2), (float("-inf"), 0)
    ],
    # 毛利率（过去5年均值）
    "gross_margin": [
        (0.60, 10), (0.45, 8), (0.30, 6), (0.20, 4), (0.10, 2), (float("-inf"), 0)
    ],
    # 毛利率稳定性（变异系数 CV，越低越稳定）
    "gross_margin_cv": [
        (0.05, 10), (0.10, 8), (0.20, 6), (0.30, 4), (0.50, 2), (float("inf"), 0)
    ],
    # 资本支出/净利润（越低越轻资产）
    "capex_to_net_income": [
        (0.10, 10), (0.25, 8), (0.50, 6), (0.75, 4), (1.0, 2), (float("inf"), 0)
    ],
    # FCF/净利润（利润质量）
    "fcf_to_net_income": [
        (1.0, 10), (0.8, 8), (0.6, 6), (0.4, 4), (0.2, 2), (float("-inf"), 0)
    ],
}

# ─────────────────────────────────────────────
# 打分阈值：财务健康
# ─────────────────────────────────────────────
FINANCIAL_HEALTH_THRESHOLDS = {
    # 流动比率
    "current_ratio": [
        (3.0, 10), (2.0, 8), (1.5, 6), (1.0, 4), (0.8, 2), (float("-inf"), 0)
    ],
    # 负债/权益
    "debt_to_equity": [
        (0.3, 10), (0.5, 8), (1.0, 6), (2.0, 4), (3.0, 2), (float("inf"), 0)
    ],
    # 利息覆盖倍数
    "interest_coverage": [
        (10.0, 10), (5.0, 8), (3.0, 6), (1.5, 4), (1.0, 2), (float("-inf"), 0)
    ],
    # 经营现金流/总负债
    "ocf_to_debt": [
        (0.5, 10), (0.3, 8), (0.2, 6), (0.1, 4), (0.05, 2), (float("-inf"), 0)
    ],
}

# ─────────────────────────────────────────────
# 打分阈值：成长性
# ─────────────────────────────────────────────
GROWTH_THRESHOLDS = {
    # 营收5年CAGR
    "revenue_cagr_5y": [
        (0.20, 10), (0.15, 8), (0.10, 6), (0.05, 4), (0.0, 2), (float("-inf"), 0)
    ],
    # 净利润5年CAGR
    "net_income_cagr_5y": [
        (0.20, 10), (0.15, 8), (0.10, 6), (0.05, 4), (0.0, 2), (float("-inf"), 0)
    ],
    # FCF 3年CAGR
    "fcf_cagr_3y": [
        (0.20, 10), (0.15, 8), (0.10, 6), (0.05, 4), (0.0, 2), (float("-inf"), 0)
    ],
    # EPS 5年CAGR
    "eps_cagr_5y": [
        (0.20, 10), (0.15, 8), (0.10, 6), (0.05, 4), (0.0, 2), (float("-inf"), 0)
    ],
}

# ─────────────────────────────────────────────
# 打分阈值：管理层
# ─────────────────────────────────────────────
MANAGEMENT_THRESHOLDS = {
    # ROIC（过去5年均值）
    "roic": [
        (0.20, 10), (0.15, 8), (0.10, 6), (0.08, 4), (0.05, 2), (float("-inf"), 0)
    ],
    # 股权稀释率（近3年年均，越低越好）
    "dilution_rate": [
        (0.00, 10), (0.01, 8), (0.02, 6), (0.03, 4), (0.05, 2), (float("inf"), 0)
    ],
    # 回购/市值（近3年年均）
    "buyback_yield": [
        (0.05, 10), (0.03, 8), (0.01, 6), (0.0, 4), (float("-inf"), 2)
    ],
    # FCF转化率（经营现金流/净利润）
    "fcf_conversion": [
        (1.2, 10), (1.0, 8), (0.8, 6), (0.6, 4), (0.4, 2), (float("-inf"), 0)
    ],
}

# ─────────────────────────────────────────────
# 打分阈值：估值
# ─────────────────────────────────────────────
VALUATION_THRESHOLDS = {
    # PEG比率（彼得·林奇核心指标）
    "peg_ratio": [
        (0.5, 10), (1.0, 8), (1.5, 6), (2.0, 4), (3.0, 2), (float("inf"), 0)
    ],
    # PE比率（行业相对）
    "pe_ratio": [
        (10, 10), (15, 8), (20, 6), (25, 4), (35, 2), (float("inf"), 0)
    ],
    # EV/EBITDA
    "ev_to_ebitda": [
        (8, 10), (12, 8), (16, 6), (20, 4), (30, 2), (float("inf"), 0)
    ],
    # PB比率
    "pb_ratio": [
        (1.0, 10), (2.0, 8), (3.0, 6), (5.0, 4), (10.0, 2), (float("inf"), 0)
    ],
}

# ═════════════════════════════════════════════════════════════════════
# V2 打分体系 — 基于巴菲特致股东信 + 段永平投资问答录
#
# 核心改进：
#   1. 一致性优先：连续多年高 ROE/ROIC 比均值更重要
#   2. FCF 视角：Owner's Earnings 理念，现金流质量为核心
#   3. 轻资产偏好：资本密集型业务天然降分
#   4. 护城河代理：毛利率稳定性 + 定价权（利润率趋势）
#   5. 估值弱化：避免过度偏好低 PE 的价值陷阱
#   6. 成长质量：有利润支撑的成长 > 纯营收成长
# ═════════════════════════════════════════════════════════════════════

# ─────────────────────────────────────────────
# V2 版本标识
# ─────────────────────────────────────────────
SCORING_VERSION = "v2"  # "v1" 切回旧版

# ─────────────────────────────────────────────
# V2 五大评分维度默认权重
# ─────────────────────────────────────────────
V2_DIMENSION_WEIGHTS = {
    "business_quality": 0.35,   # 提升：护城河+现金流质量是核心
    "financial_health": 0.20,   # 不变
    "growth":           0.15,   # 降低：避免追高成长股
    "management":       0.20,   # 提升：资本配置能力
    "valuation":        0.10,   # 降低：弱化估值，避免价值陷阱
}

# ─────────────────────────────────────────────
# V2 四位大师权重矩阵
# ─────────────────────────────────────────────
V2_MASTER_WEIGHTS = {
    "buffett": {
        "business_quality": 0.40,
        "financial_health": 0.15,
        "growth":           0.10,
        "management":       0.25,
        "valuation":        0.10,
        # 巴菲特致股东信核心：demonstrated consistent earning power
        # + good returns on equity with little debt + simple businesses
    },
    "munger": {
        "business_quality": 0.35,
        "financial_health": 0.15,
        "growth":           0.10,
        "management":       0.35,
        "valuation":        0.05,
        # 芒格核心：激励机制 + 不做蠢事 + ROIC 导向
    },
    "duan": {
        "business_quality": 0.40,
        "financial_health": 0.10,
        "growth":           0.20,
        "management":       0.30,
        "valuation":        0.00,
        # 段永平核心：好生意+好企业+好价格（但不靠估值指标选股）
        # "商业模式最重要" + "产品差异化" + "10年20年后还在"
    },
    "lynch": {
        "business_quality": 0.20,
        "financial_health": 0.15,
        "growth":           0.35,
        "management":       0.10,
        "valuation":        0.20,
        # 林奇核心：PEG + 被忽视的成长股
    },
}

# ─────────────────────────────────────────────
# V2 一票否决项（更严格）
# ─────────────────────────────────────────────
V2_VETO_CONDITIONS = {
    "fcf_negative_years": 2,            # 连续2年FCF为负（v1=3，太宽松）
    "net_loss_consecutive_years": 2,    # 同v1
    "debt_to_fcf_max": 8.0,            # 8年偿债上限（v1=10，巴菲特偏好低杠杆）
    "roe_below_cost_of_capital": 0.08,  # 新增：5年均ROE低于8%说明资本配置差
    "gross_margin_collapse": -0.15,     # 新增：毛利率3年下滑超15pct（失去定价权）
}

# ─────────────────────────────────────────────
# V2 打分阈值：生意质量（护城河+现金流品质）
# 来源：巴菲特"demonstrated consistent earning power"
#        段永平"好生意模式=有差异化产品+定价权"
# ─────────────────────────────────────────────
V2_BUSINESS_QUALITY = {
    # ROE 一致性：5年中有几年 > 15%（比均值更重要）
    "roe_consistency": [
        (5, 10), (4, 8), (3, 6), (2, 4), (1, 2), (0, 0)
    ],
    # ROE 5年均值（辅助指标）
    "roe_avg": [
        (0.25, 10), (0.20, 8), (0.15, 6), (0.10, 4), (0.05, 2), (float("-inf"), 0)
    ],
    # 毛利率均值（定价权代理）
    "gross_margin": [
        (0.60, 10), (0.45, 8), (0.30, 6), (0.20, 4), (0.10, 2), (float("-inf"), 0)
    ],
    # 毛利率稳定性 CV（越低越好 = 护城河更宽）
    "gross_margin_cv": [
        (0.03, 10), (0.06, 8), (0.10, 6), (0.20, 4), (0.35, 2)
    ],
    # FCF/营收比（Owner's Earnings 视角，比 FCF/NI 更稳健）
    "fcf_to_revenue": [
        (0.20, 10), (0.15, 8), (0.10, 6), (0.05, 4), (0.02, 2), (float("-inf"), 0)
    ],
    # 轻资产度：Capex/营收（段永平："资本支出大的行业不容易出好企业"）
    "capex_to_revenue": [
        (0.02, 10), (0.04, 8), (0.08, 6), (0.15, 4), (0.25, 2)
    ],
}

# ─────────────────────────────────────────────
# V2 打分阈值：财务健康（巴菲特偏好极低杠杆）
# 来源：巴菲特"good returns on equity while employing little or no debt"
# ─────────────────────────────────────────────
V2_FINANCIAL_HEALTH = {
    # 净负债/FCF（真实偿债能力，比 D/E 更有意义）
    "net_debt_to_fcf": [
        (0.0, 10), (1.0, 8), (2.0, 7), (3.0, 5), (5.0, 3), (8.0, 1)
    ],
    # 流动比率
    "current_ratio": [
        (3.0, 10), (2.0, 8), (1.5, 6), (1.0, 4), (0.8, 2), (float("-inf"), 0)
    ],
    # 利息覆盖倍数
    "interest_coverage": [
        (15.0, 10), (8.0, 8), (5.0, 6), (3.0, 4), (1.5, 2), (float("-inf"), 0)
    ],
    # 经营现金流是否持续为正（过去5年有几年为正）
    "ocf_positive_years": [
        (5, 10), (4, 8), (3, 5), (2, 2), (1, 1), (0, 0)
    ],
}

# ─────────────────────────────────────────────
# V2 打分阈值：成长质量（有利润支撑的成长）
# 来源：段永平"想10年20年后" + 巴菲特偏好稳定成长
# ─────────────────────────────────────────────
V2_GROWTH_QUALITY = {
    # 营收 CAGR 5年（门槛降低，5%稳定成长即为良好）
    "revenue_cagr_5y": [
        (0.15, 10), (0.10, 8), (0.07, 6), (0.04, 4), (0.0, 2), (float("-inf"), 0)
    ],
    # EPS CAGR 5年（盈利成长比营收成长更重要）
    "eps_cagr_5y": [
        (0.15, 10), (0.10, 8), (0.07, 6), (0.04, 4), (0.0, 2), (float("-inf"), 0)
    ],
    # 营收成长一致性：过去5年有几年正增长
    "revenue_growth_consistency": [
        (5, 10), (4, 8), (3, 6), (2, 3), (1, 1), (0, 0)
    ],
    # 利润率趋势：近3年vs前3年净利润率变化（定价权/效率信号）
    # 正值=改善，负值=恶化
    "margin_trend": [
        (0.05, 10), (0.02, 8), (0.0, 6), (-0.02, 4), (-0.05, 2), (float("-inf"), 0)
    ],
}

# ─────────────────────────────────────────────
# V2 打分阈值：资本配置（管理层+股东回报）
# 来源：巴菲特"management in place" + 段永平"管理者正直诚信"
# ─────────────────────────────────────────────
V2_CAPITAL_ALLOCATION = {
    # ROIC 一致性：5年中有几年 > 12%（超越资本成本）
    "roic_consistency": [
        (5, 10), (4, 8), (3, 6), (2, 4), (1, 2), (0, 0)
    ],
    # ROIC 5年均值
    "roic_avg": [
        (0.25, 10), (0.20, 8), (0.15, 7), (0.10, 5), (0.08, 3), (float("-inf"), 0)
    ],
    # 股权稀释率（越低越好，巴菲特偏好回购）
    "dilution_rate": [
        (-0.02, 10), (0.00, 8), (0.01, 6), (0.02, 4), (0.03, 2)
    ],
    # 股东总回报率（回购+分红/市值）
    "shareholder_yield": [
        (0.06, 10), (0.04, 8), (0.02, 6), (0.01, 4), (0.0, 2), (float("-inf"), 0)
    ],
    # FCF 转化率（经营现金流/净利润 - 利润质量）
    "fcf_conversion": [
        (1.2, 10), (1.0, 8), (0.8, 6), (0.6, 4), (0.4, 2), (float("-inf"), 0)
    ],
}

# ─────────────────────────────────────────────
# V2 打分阈值：估值（弱化，避免价值陷阱）
# 来源：段永平"定性比定量分析重要" + "FCF折现是思维方式不是公式"
# 巴菲特"we prefer to buy for cash"
# ─────────────────────────────────────────────
V2_VALUATION = {
    # FCF 收益率（Owner's Earnings 视角 — 最重要的估值指标）
    "fcf_yield": [
        (0.08, 10), (0.06, 8), (0.04, 6), (0.03, 4), (0.02, 2), (float("-inf"), 0)
    ],
    # 盈利收益率 1/PE（与国债利率对比）
    "earnings_yield": [
        (0.08, 10), (0.06, 8), (0.05, 6), (0.04, 4), (0.03, 2), (float("-inf"), 0)
    ],
    # PEG（仅当 EPS 增长率为正时有效）
    "peg_ratio": [
        (0.8, 10), (1.0, 8), (1.5, 6), (2.0, 4), (3.0, 2)
    ],
    # EV/EBITDA（放宽阈值，不过度惩罚优质公司）
    "ev_to_ebitda": [
        (10, 10), (14, 8), (18, 6), (22, 4), (30, 2)
    ],
}

# ─────────────────────────────────────────────
# 输出配置
# ─────────────────────────────────────────────
OUTPUT_CONFIG = {
    "scores_dir": "scores",
    "raw_data_dir": "data/raw",
    "summary_filename": "_summary.csv",
    "run_log_filename": "_run_log.txt",
    "sleep_between_tickers": 2.0,   # 限速保护，约10 req/s
}
